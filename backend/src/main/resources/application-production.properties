spring.application.name=demo

# Conexión a la base de datos
spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/${MYSQL_DATABASE}?useSSL=false&serverTimezone=UTC
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQL_ROOT_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Puerto del servidor (puedes omitirlo si Railway lo gestiona automáticamente)
server.port=8080

# Configuración del correo (considera ocultar esta info en producción)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sferehi18@gmail.com
spring.mail.password=gwvh dryy oefp whnl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuración JPA e Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# Archivos grandes
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=1GB
spring.servlet.multipart.max-request-size=1GB
